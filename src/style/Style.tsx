import { extendTheme } from 'native-base';

export const Style = extendTheme({
    colors:{
        gray: {
            300: '#8D899'
        },
        blue: {
            500: '#339CFF'
        }
    }
})